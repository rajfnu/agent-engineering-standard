---
description: Run a scoped engineering review of the current branch, a PR, or a directory
argument-hint: "[branch | PR number | path]  (default: current branch vs. main)"
---

Use the `engineering-review` skill to review: $ARGUMENTS

If no target was given, review the current branch against its merge base with the default
branch.

Follow the skill's pipeline. Do not restate its instructions here — load the skill.
